export enum LocalStorageKey {
  PUSHKA_LANGUAGE = 'pushka-language',
  PUSHKA_DEV_MODE = 'pushka-dev-mode',
  APP_CURRENT_LANGUAGE = 'app-current-language',
  APP_CURRENT_LOCALE = 'app-current-locale',
  USER_TOKEN = 'user-token',
}
