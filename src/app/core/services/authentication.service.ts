import { Injectable } from '@angular/core';

// TODO: !IMPORTANT Finish the service
@Injectable({
  providedIn: 'root',
})
export class AuthenticationService {
  public isUserLoggedIn(): boolean {
    return false;
  }
}
